{
  "name": "coway-gallery-builder",
  "private": true,
  "version": "1.0.0",
  "description": "사진 폴더에서 갤러리 목록과 웹용 이미지를 자동 생성합니다.",
  "engines": { "node": ">=22" },
  "scripts": { "build": "node build.cjs", "test": "node --test test.cjs" },
  "dependencies": { "sharp": "0.35.4" }
}
